# Server Configuration
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# Optional: Database connection (for future use)
# DATABASE_URL=postgresql://user:password@localhost:5432/fastify_demo

# Optional: JWT Secret (for future authentication)
# JWT_SECRET=your-secret-key-here

# Optional: Logging level
# LOG_LEVEL=info